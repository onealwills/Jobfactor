import React from 'react';
import { Box, Paper } from '@mui/material';
import SectionHeader from '../../common/SectionHeader';

const SentRequests = () => {
  return (
    <>
      <SectionHeader header={'Test'} subHeader={'test test test'} />
    </>
  );
};

export default SentRequests;
