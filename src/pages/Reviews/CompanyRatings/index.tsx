import React from 'react';
import SectionHeader from '../../common/SectionHeader';

const CompanyRatings = () => {
    return <SectionHeader header={'Company Ratings'} />;
};

export default CompanyRatings;
